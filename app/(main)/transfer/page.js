import TransferMain from "@/components/layout/transfer/TransferMain";
import { Suspense } from "react";

export default function Transfer() {
  return (
    <Suspense>
      <TransferMain />
    </Suspense>
  );
}
